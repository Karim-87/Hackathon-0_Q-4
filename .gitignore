# Environment secrets
.env
.env.*
!.env.example

# Python-generated files
__pycache__/
*.py[oc]
*.pyc
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Obsidian workspace (user-specific)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# OAuth credentials and tokens — NEVER commit these
secrets/
credentials.json
*_token.json
*.credentials
*.session
*.key
*.pem

# Audit logs (contain action details — keep local only)
Logs/audit/

# Node
node_modules/

# WhatsApp session data — NEVER commit (contains auth tokens)
.wwebjs_auth/
.wwebjs_cache/
whatsapp_media/

# WhatsApp media downloads
Needs_Action/media/
